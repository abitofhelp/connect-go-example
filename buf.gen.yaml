# This content is only used by buf's cli and the buf_lint_test in Bazel.
# It it NOT used to determine Bazel's generation of code and output directories.
version: v1
plugins:
  - plugin: go
    out: gen
    opt: paths=source_relative
  - plugin: connect-go
    out: gen
    opt: paths=source_relative